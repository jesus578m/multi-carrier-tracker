
{
  "name": "multi-carrier-tracker",
  "version": "1.0.0",
  "description": "Simple multi-carrier tracking application using public tracking pages (no API keys)",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node server.is
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.42.0",
    "serverless-http": "^3.0.1"
  }
}
